import { APIPayload } from '../../common/v2.js';
import '../../utils.js';

/**
 * @see https://docs.squarecloud.app/api-reference/endpoint/service/status
 */
interface APIServiceStatus {
    status: string;
    message: string;
}
type APIServiceStatusPayload = APIPayload<APIServiceStatus>;

export type { APIServiceStatus, APIServiceStatusPayload };
