import { Brand } from '../utils.js';

type APIPayloadStatus = "error" | "success";
interface APIPayload<TResponse> {
    status: APIPayloadStatus;
    response: TResponse;
    code?: string;
}
interface APIPayloadStatusOnly {
    status: APIPayloadStatus;
}
interface APIHeaders {
    Authorization: string;
}
type APIVersion<TVersion extends number> = `v${TVersion}`;
type ISODateString = string;
type UserId = string & Brand<"UserId">;
declare const UserId: (id: string) => UserId;
type ApplicationId = string & Brand<"ApplicationId">;
declare const ApplicationId: (id: string) => ApplicationId;

export { type APIHeaders, type APIPayload, type APIPayloadStatus, type APIPayloadStatusOnly, type APIVersion, ApplicationId, type ISODateString, UserId };
